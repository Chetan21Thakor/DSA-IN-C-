#include<iostream>
using namespace std;

int main(){
    int age=20;
    char grade='A';
    bool isAdult=true;
    double height=5.9;
    float weight=70.5;

    //* this are the different data types in C++ and their usage. 
    // *This are the Primitives data types in C++. int(integer) store a integer value, char(character) character store single character, bool(boolean) boolean store a boolean value, double(double precision floating point) double store a double precision floating point value, float(single precision floating point) float store a single precision floating point value.
}