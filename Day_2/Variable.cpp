#include<iostream>
using namespace std;
 

int main(){
    int a=10;// this line tell to compiler take memory location give name that memory location to "a" and store integer value 10.
    //? 10 is literal . Literal has fix value and meaning which can't change. just like constant. for example here 10 is literal which meaning always 10 and value is always equal to 10.
    //? Name of variable,class,object,structure,union etc.. which used to identify them it's called identifier. 
    // ? tip :"Always give meaningful name to a identifier."
    // int b;
    // b=5;
    // int c,d;
    // c=11,d=12;
    // int e=6,f=8;
    // int g,h,i=21;
    // g=h=2;
}