#include<iostream>
using namespace std;

int main(){
    // * Syntax of Creating a constant

    const float  pi=3.14;//* you must have to initialize it.
    //* you can not change it's value in entire Program.
    //* you can use like normal variable.
    //* it's take  space in memory and created just like normal variable in memory. just because of that we have to define it's datatype.

    cout<<"Area of circle is which radius is 4:"<<"\t"<<2*pi*4; 
}