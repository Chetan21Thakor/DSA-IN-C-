#include<iostream>
#define PI 3.14
#define ll long long
using namespace std;

int main(){

    cout<<"Micros value"<<PI<<endl;
    int y=8;
    ll int x=9;
    // cout<<"size of a long long int "<<sizeof(x);
    // cout<<"size of a long long int "<<sizeof(y);

    //*1.it's a Preprocessor Directive.
    //*2.it's define outside of the main function.
    //* 3.it's not taking any extra space in memory.
    //* 4.we no need to define it's data type.
    //* 5.it's basically used to create a data type modifier or data type symbolic constants.
    //* 6.if we are use micros like variable inside a Program or inside main function it's value evaluate before starting the execution of Program. 
}